 A[Start] --> B[Input P, R, and T: Take the principal, rate, and time as inputs]
    B --> C[Calculate SI: Use the formula SI = (P * R * T) / 100]
    C --> D[Print SI: Display the calculated simple interest]
    D --> E[End]
